<p id="notice"><%= notice %></p>

<h1>All Users</h1>
<%= will_paginate %>
<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <%= render @users %>
 
  </tbody>
</table>
<%= will_paginate %>

<%= link_to 'New User', new_user_path %>
